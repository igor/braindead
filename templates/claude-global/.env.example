# Environment Variables for Claude Code and RAG Infrastructure
# Copy this file to .env and fill in your actual values
# NEVER commit .env to git - it's in .gitignore

# RAG Infrastructure
RAG_HOST={YOUR_RAG_HOST}  # e.g., localhost, macmini.local, or IP address
RAG_PORT={YOUR_RAG_PORT}  # e.g., 8000
RAG_PATH={YOUR_RAG_PATH}  # e.g., /path/to/rag/storage

# MCP Server Configuration
MCP_SERVER_PORT={YOUR_MCP_PORT}  # e.g., 8000

# Ollama Configuration
OLLAMA_HOST={YOUR_OLLAMA_HOST}  # e.g., http://localhost:11434
OLLAMA_VERSION={YOUR_OLLAMA_VERSION}  # e.g., 0.13.5

# Embedding Model
EMBEDDING_MODEL={YOUR_MODEL}  # e.g., nomic-embed-text

# API Keys (if using external services)
# PERPLEXITY_API_KEY={YOUR_KEY}
# ANTHROPIC_API_KEY={YOUR_KEY}
# OPENAI_API_KEY={YOUR_KEY}

# Paths
VAULT_PATH={YOUR_VAULT_PATH}  # e.g., /Users/username/Documents/vault
VAULT_NAME={YOUR_VAULT_NAME}  # e.g., my-knowledge-base

# SSH Configuration (if RAG is on remote machine)
# RAG_SSH_HOST={YOUR_SSH_HOST}
# RAG_SSH_USER={YOUR_SSH_USER}
# RAG_SSH_KEY={PATH_TO_SSH_KEY}

# Custom Variables
# {CUSTOM_VAR_1}={VALUE}
# {CUSTOM_VAR_2}={VALUE}
